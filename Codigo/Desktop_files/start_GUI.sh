#!/bin/bash
# Este script se ejecuta cada vez que se presiona el boton de iniciar el programa
# Se añadio por el problema de la referencia de los archivos ".glade"
# De esta manera ya no es necesario agregarlos al propio ejecutable
cd $HOME/.pccae32/GUI/exec/ && ./executable
